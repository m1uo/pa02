#ifndef MOVIES_H
#define MOVIES_H

#include <string>
#include <utility>

bool compareMovies(const std::pair<std::string, double>& a,
                   const std::pair<std::string, double>& b);

#endif
